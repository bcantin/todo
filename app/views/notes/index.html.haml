%h2= @project.title

%p.description
  = @project.description
  = link_to 'Tasks', @project

%h3 Notes
%ul#notes.notes
  - @project.notes.each do |note|
    = render :partial => 'notes/note', :locals => {:note => note}

%hr

= render :partial => 'form'
