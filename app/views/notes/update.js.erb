var update_id = "#note_" + <%= @note.id %>
$('#notes').find(update_id).replaceWith("<%= escape_javascript(
    render :partial => 'note', :locals => {:note => @note}
  ) %>");
jQuery(document).trigger('close.facebox')