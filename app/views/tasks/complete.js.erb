// remove the old
var destroyed_id = "#task_" + <%= @task.id %>
$(destroyed_id).remove();

// render the new


$('#completed').prepend("<%= escape_javascript(
    render :partial => 'completed_tasks', :locals => {:task => @task}
  ) %>");

$('#completed li').first().effect('highlight');